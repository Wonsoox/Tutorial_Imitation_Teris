DesignDiagram
```mermaid
graph TD
  GameOver[게임오버] --> Restart[재시작]
  Restart --> GameStart[게임시작]
  GameStart --> Play[플레이]
  Play --> Clear[(버전마다 다르지만 특정조건을 만족할때까지 패배하지 않고 버텼을 시, 다음레벨로)]
  Clear --> Head2HigherLevel[다음단계로]
  Head2HigherLevel --> Play
  Play --> LoseCondition[테트리스블럭이 한계선을 지나면 패배]
  LoseCondition --> GameOver
```
RuntimeDiagram
```mermaid
graph TD
  A --> B
```
ClassDiagram
```mermaid

```
